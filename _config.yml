title: Anders Pollack
baseurl: ""
port: 4001

defaults:
  - scope:
      path: ""
    values:
      layout: default

  - scope:
      path: "assets/images"
    values:
      image: true

collections:
  project_collections:
    output: true
    permalink: /projects/:name
